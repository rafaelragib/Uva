#include<bits/stdc++.h>
using namespace std;

int main()
{
    int l;
    ifstream in;
    in.open("data.txt");
    while(in >> l)
    {
        //getchar();
        in.ignore();
        if(l==0)
            return 0;

        string input;

    }
    return 0;
}
