#include<iostream>
#include<vector>
using namespace std;
int main()
{
    int n,m;
    cin >> n>> m;
    int b[n],cd[n],d[n],d_lower[2*n],d_uper[2*n];

    for(int i=0,j=0;i<n;i++)
    {
        string name_in;
        vector<string> name;
        name.push_back(name_in);
        cin >> b[i] >> cd[n] >> d[n];
        for( ;j<d[j];j++)
        {
            cin >> d_lower[j] >>d_uper[j];
        }

    }
}
